{
  "name": "studio-m",
  "version": "1.1.0",
  "description": "پلتفرم مدیریت استودیو MAN",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-static.mjs",
    "preview": "vite preview",
    "lint": "eslint js/ studio-m/js/ scripts/ contract.js eslint.config.js eslint.globals.js --max-warnings 0 --no-warn-ignored",
    "validate:repo": "node scripts/validate-repository.mjs",
    "ci:verify": "npm run validate:repo && npm run validate:release && npm run validate:test-quality && npm run security:scan && npm run lint && npm test && npm run build && npm run validate:build",
    "validate:release": "node scripts/validate-release.mjs",
    "validate:build": "node scripts/validate-build.mjs",
    "validate:test-quality": "node scripts/validate-test-quality.mjs",
    "sbom": "node scripts/generate-sbom.mjs",
    "security:scan": "node scripts/security-scan.mjs",
    "test:load": "node scripts/load-smoke.mjs",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:e2e": "npm run test:e2e:visual && npm run test:e2e:auth",
    "test:e2e:visual": "E2E_LOCAL_DEMO_BUILD=1 npm run build && playwright test e2e/visual.spec.js",
    "test:e2e:auth": "npm run build && playwright test e2e/finance-auth.spec.js",
    "test:e2e:ui": "playwright test --ui",
    "test:realtime:two-device": "node scripts/two-device-realtime-test.mjs",
    "test:rls:local": "supabase db reset --local && supabase test db && bash scripts/test-assignment-concurrency.sh"
  },
  "engines": {
    "node": ">=22 <25"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "@vitest/coverage-v8": "4.1.11",
    "eslint": "10.9.0",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.3"
  }
}
